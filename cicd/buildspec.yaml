version: 0.2

phases:
    install:
        runtime-versions:
            nodejs: latest
    pre-build:
        commands:
            - echo "installing dependencies"
            - npm install
    build:
        commands:
            - echo "do nothing"
    post_build:
        commands:
            - echo "Build completed on $(date)"
artifacts:
    files:
        - "**/*"
    base-directory: '/'
    discard-paths: no